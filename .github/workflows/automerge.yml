name: automerge

on:
  push:
    branches:
      - master

  pull_request:
    branches:
      - master

jobs:
  build:
    name: Gitflow Automation
    runs-on: ubuntu-latest

    steps:
      - name: automerge1
        run: echo Test11...
      - name: automerge2
        run: git pull origin/master release
